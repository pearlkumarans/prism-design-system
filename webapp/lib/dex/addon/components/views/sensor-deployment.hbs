{{! Sensor deployments — a DEX list (L03), a thin Patterns::ListView instance. }}
<Patterns::ListView
  @header={{this.header}}
  @fetch={{this.fetch}}
  @columns={{this.columns}}
  @facets={{this.facets}}
  @kpis={{this.kpis}}
/>
