{{! Missing Patches — a Threats & Patches list (L03), a thin Patterns::ListView
    instance. No breadcrumb (first-level nav landing). }}
<Patterns::ListView
  @header={{this.header}}
  @fetch={{this.fetch}}
  @columns={{this.columns}}
  @facets={{this.facets}}
  @kpis={{this.kpis}}
  @bulkActions={{this.bulkActions}}
  @onBulkAction={{this.onBulkAction}}
/>
