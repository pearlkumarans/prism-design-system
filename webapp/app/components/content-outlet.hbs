{{!
  ContentOutlet — the ds-content mount. Binds to the routed @slug and lets the
  mount-view modifier fetch + inject the real dual-mode view file into this host.
  Template-only: all behavior lives in the modifier (the injectDrawer port).
}}
<div class="content-outlet" {{mount-view @slug}}></div>
