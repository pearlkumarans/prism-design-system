{
  "name": "prism-design-system",
  "version": "1.0.0",
  "private": true,
  "description": "Prism Design System — app shell + dual-mode views, served with a dev proxy to the Endpoint Central backend.",
  "scripts": {
    "serve": "node server/dev-proxy.mjs",
    "start": "node server/dev-proxy.mjs",
    "test:api": "node --test server/test/"
  },
  "engines": {
    "node": ">=18"
  }
}
